# Multi-Field Selector

**Multi-Field Selector** is a Power BI custom visual that lets report users select and filter across multiple related fields within one visual.

Add fields such as:

- Region → Office → Team
- Category → Product → Variant
- Service → Journey → Stage
- Year → Month → Week

The fields do not need to be configured as a formal Power BI hierarchy.

## Current features

- Multiple related fields displayed in one selector
- Single and multiple selection modes
- Select all compatible or matching values in Multiple mode
- Space-efficient column layout for dashboard headers
- Context-aware values across fields
- Configurable, delayed hierarchy-path tooltips on hover and keyboard focus
- Configurable value sorting: data order, A–Z or Z–A
- Search automatically shown for fields with enough values
- Global search visibility toggle and configurable value threshold
- Selected values pinned above scrolling lists
- Report filtering using Power BI filter APIs
- Selection persistence when reports are saved and reopened
- Bookmark support
- Clear all and per-field clear controls
- Configurable fonts, colours, spacing and containers
- Keyboard-accessible buttons, search boxes and labels

## Status

🚧 Under development

## Planned features

- Further accessibility testing
- Microsoft certification
